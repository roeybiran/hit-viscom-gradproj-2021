import styled from "styled-components";

const Switcher = styled.div`
  display: flex;
  flex-wrap: nowrap;
  gap: var(--s1);
  overflow: auto;
  /* font-size: var(--s3); */
`;

export default Switcher;
